name: Dump node version

on:
  workflow_dispatch:
  push:
    branches:
      - "master"

jobs:
  build:
    name: Dump node version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Dump node version
        run: node --version
